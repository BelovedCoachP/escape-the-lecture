{
  "name": "escape-the-lecture",
  "version": "0.1.0",
  "private": true,
  "description": "Accessible escape-room engine: two renderers, one JSON content contract. Accessibility lives in the schema.",
  "type": "module",
  "scripts": {
    "validate": "node scripts/validate.mjs",
    "prepare": "husky"
  },
  "author": "Page Durham",
  "devDependencies": {
    "ajv": "^8.20.0",
    "husky": "^9.1.7"
  }
}
